# Library Management Tool (C)

## What it does
Stores a dataset of books using structural components and allows users to search for specific entries instantly using unique identification numbers.

## Concepts Used
- Array of Structures
- Linear Search Logic
- String Copying (`strcpy`)

## How to run it
1. Compile the file: `gcc library.c -o library`
2. Run the executable: `./library`
